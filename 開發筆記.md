# 開發筆記：Excel → OpenStack 安全群組匯入工具

這份筆記記錄 `sg_firewall_v3.go` 的設計思路、打包成 exe 的流程、用到的工具，以及背後幾個可以帶著走的通用觀念。

---

## 1. 這個工具在做什麼

把 Excel（`planweb_policy.xlsx`）裡的防火牆規則，透過 AI-TRUST 平台的 API，一次全部建立成 OpenStack 安全群組（Security Group）規則。因為規則有幾十上百條，手動在網頁一條一條建太慢，所以走 API 批次處理。

資料流：

```
Excel 規則  ──讀取──▶  分組成 SecGroup  ──比對既有──▶  呼叫 API 建立/略過/刪除
(excelize)            (以 Rule Name 分組)   (idempotent)      (net/http)
```

---

## 2. 程式碼核心設計

### 2-1. 讀 Excel（excelize 套件）

`loadSecGroupsFromExcel()` 用 `github.com/xuri/excelize/v2` 開檔、讀第一個工作表的所有列。
先讀標題列建立「欄名 → 欄索引」的對照表，這樣**不管欄位順序怎麼變，都用欄名抓值**，比寫死欄號穩。

```go
col := map[string]int{}
for i, h := range header { col[strings.TrimSpace(h)] = i }
```

### 2-2. 分組：同一個 Rule Name = 同一個安全群組

用一個 `map[string]*SecGroup` 收集，同時用一個 `order []string` 記住第一次出現的順序，
最後照 `order` 輸出——**map 本身無序，要另外記順序**，這是 Go 很常見的小技巧。

### 2-3. 方向判斷（優先讀「方向」欄）

`directionFromColumn()` 先讀 Excel 的「方向」欄（`ingress`/`egress`）；
如果那欄不存在或留空，才退回用 `directionFor()` 看 Rule Name 結尾（`in`/`2ex`）。

有個容易踩的坑：取選填欄時不能直接用共用的 `get()`，因為欄位不存在時 `col["方向"]` 會回傳 0、
反而抓到第 0 欄。所以要先確認欄位存在：

```go
if idx, ok := col["方向"]; ok && idx < len(row) {
    direction = directionFromColumn(row[idx])
}
```

### 2-4. Port 解析（吃三種格式）

`parsePortService()` 能處理：純數字 `443`、範圍 `443:478` / `443-478`、服務名 `HTTPS port:443`。
回傳 `portMin`/`portMax`（整數，給 API）和 `portSpec`（字串，給畫面顯示）。

### 2-5. 冪等（idempotent）設計 —— 整支程式最重要的觀念

重跑很多次也不會重複建立，靠兩個東西：

1. **穩定 key**：`ruleKey()` 把「方向 / 協定 / port / remote_cidr」串起來算 SHA-256，取前 12 碼。
   只跟規則的「本質內容」有關，**跟 Excel 列號、順序無關**——所以在 Excel 插入/刪除/搬動列，
   同一條規則永遠對到同一把 key。

   ```go
   raw := fmt.Sprintf("%s|%s|%d|%d|%s", direction, protocol, portMin, portMax, remoteCIDR)
   sum := sha256.Sum256([]byte(raw))
   return hex.EncodeToString(sum[:])[:12]
   ```

2. **管理標記**：每條規則的 description 都塞一段 `[xlsx-managed:<key>]`。
   下次執行時，先 GET 平台上現有規則、用正則把 key 抓回來，就能判斷：
   - Excel 有、平台沒有 → **新增**
   - 兩邊都有 → **略過**
   - 平台有、Excel 沒有 → **孤兒規則**（加 `--prune` 才會刪，預設只警告）

   > 這也是為什麼你「改了 port 直接重跑卻沒生效」——舊規則 key 沒變、被判定已存在而略過，
   > 所以要先把舊規則刪掉再跑。

### 2-6. 呼叫 API（net/http）

`doRequest()` 是一個薄薄的 HTTP 包裝：帶 `Authorization: Bearer <token>`、處理 409（當成「已存在」）、
4xx 以上回傳錯誤。正確的 rule payload 欄位是（**這是這次除錯的關鍵**）：

```json
{ "direction": "ingress", "network_type": "Ipv4",
  "port_min": 443, "port_max": 443,
  "protocol": "tcp", "remote_cidr": "140.110.35.0/24" }
```

一開始猜 `port_range_min/max`、又猜單一 `port`，都被 API 默默忽略（規則建了但 port 空白）。
最後是**用瀏覽器 F12 → Network 攔手動建立時真正送出的請求**，才看到正確欄位名。
→ 教訓：**除錯 API 時，與其猜文件，不如直接看正在運作的真實流量。**

---

## 3. 設定與憑證外部化

原本 token / project_id 是寫死在原始碼裡的——這在要發給別人、或推上 git 時是大忌。改成：

- 憑證放在 exe 旁邊的 `config.yaml`（`gopkg.in/yaml.v3` 解析）。
- 用 `os.Executable()` 找 exe 自己的所在目錄，**這樣使用者雙擊 exe（工作目錄可能在別處）也找得到設定檔**。
- 優先序：環境變數 > config.yaml > 預設值。
- 範本裡「請填入…」這種佔位字串會被當成未填，避免拿假值去打 API。

---

## 4. 打包成可發佈的 exe

### 4-1. 編譯

Go 的殺手鐧：**編出來就是單一靜態執行檔，目標電腦不用裝 Go 或任何 runtime。**

```bash
go build -ldflags "-s -w" -o dist/sg_firewall.exe ./sg_firewall_v3.go
```

- `-ldflags "-s -w"`：拿掉除錯符號表，讓 exe 檔案小一點。
- 這個 repo 裡有多支 `package main`，不能整包 build，所以指定**單一檔案**編譯。

### 4-2. 雙擊友善

- `exitPause()`：程式結束前倒數 10 秒再關閉，避免主控台視窗「一閃即逝」看不到結果。
  用環境變數 `SG_PAUSE=0` 可跳過（從終端機測試時用）。
- 附兩個 `.bat`：`1_預覽.bat`（`--dry-run`）、`2_正式匯入.bat`，讓不會用命令列的人直接雙擊。

### 4-3. 發佈包

`dist/` 資料夾：`exe + config.yaml(範本) + 範例 Excel + 兩個 bat + 使用說明.md`，壓成 zip。
**注意**：打包 zip 時用的是「佔位版」config，不會把自己填了真 token 的 config 打包出去。

---

## 5. 用到的工具

| 工具 | 用途 |
|------|------|
| **Go**（winget 安裝 `GoLang.Go`） | 編譯語言；產出跨平台靜態執行檔 |
| **excelize/v2** | 讀寫 Excel（不需要裝 Office） |
| **yaml.v3** | 解析 config.yaml |
| **net/http + crypto/sha256** | 呼叫 API、算穩定 key（都是標準庫） |
| **瀏覽器 F12 / Network** | 攔真實 API 請求，找出正確欄位格式 |
| **git + .gitignore** | 版控；忽略 `config.yaml`/`*.exe`/`*.zip` 防止外洩 |
| **grep 掃描** | 每次 push 前掃 `eyJ...`（JWT）、密碼、project_id |

---

## 6. 可以帶走的幾個通用觀念

1. **冪等（idempotency）**：批次工具要能安全地重跑。用「內容決定的穩定 ID」+「自己蓋的管理標記」，
   就能區分新增/略過/該刪，而不是每次都盲目重建。
2. **設定與程式分離**：憑證、環境相關的東西外部化到設定檔或環境變數，程式碼裡不留機密。
3. **憑證衛生**：push 前一定掃一遍原始碼有沒有寫死的 token/密碼；用 `.gitignore` 把機密檔擋在版控外。
   —— 這次就在 push 前擋下了兩批寫死的 JWT 和一組 NSX admin 密碼。
4. **除錯靠證據，不靠猜**：API 欄位對不上時，直接看瀏覽器真正送出的 payload，一翻兩瞪眼。
5. **對非技術使用者友善**：單一 exe、雙擊即用、視窗停住、設定檔有註解——好用的工具，一半是這些細節。

