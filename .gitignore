# ── 憑證 / 機密（絕對不要 commit）──
# 填了真實 project_id / token 的設定檔（範本 config.yaml.example 不受影響）
config.yaml
dist/config.yaml
*.token

# ── 編譯產物 ──
*.exe
*.dll
# 例外：發佈用的主程式要進版控，讓別台 git pull 就能直接用
!dist/sg_firewall.exe

# ── 發佈打包 ──
sg_firewall_tool.zip
*.zip

# ── 其他 ──
# 若需要保留某個被忽略的檔案，可用 git add -f 強制加入
